#!/bin/sh

set -x            # print commands before execution
set -o errexit    # always exit on error
set -o nounset    # fail on unset variables

echo This is the generate-docs buildpack

git clone git@github.com:DefactoSoftware/detroit.git
cd detroit
mix deps.get
mix docs

mv doc/* /app
